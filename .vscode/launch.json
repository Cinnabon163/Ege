{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Текущий файл",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "args": [],  // Можно добавить аргументы командной строки, если нужны
            "cwd": "${fileDirname}"
        }
    ]
}